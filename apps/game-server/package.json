{
  "name": "@foodfight/game-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm --platform node --bundle --no-external @foodfight/game-core --no-external @foodfight/maps --no-external @foodfight/protocol",
    "start": "node dist/index.js",
    "check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@colyseus/schema": "latest",
    "@colyseus/ws-transport": "latest",
    "@foodfight/game-core": "workspace:*",
    "@foodfight/maps": "workspace:*",
    "@foodfight/protocol": "workspace:*",
    "colyseus": "latest"
  },
  "devDependencies": {
    "tsx": "latest",
    "tsup": "latest"
  }
}
