{
  "name": "@foodfight/game-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "tsc -b && vite build",
    "check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@colyseus/sdk": "latest",
    "@foodfight/game-core": "workspace:*",
    "@foodfight/maps": "workspace:*",
    "@foodfight/protocol": "workspace:*",
    "playcanvas": "latest",
    "vite": "latest"
  }
}
