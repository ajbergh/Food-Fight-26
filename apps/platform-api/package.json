{
  "name": "@foodfight/platform-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm --platform node",
    "start": "node dist/index.js",
    "check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "fastify": "latest"
  },
  "devDependencies": {
    "tsx": "latest",
    "tsup": "latest"
  }
}
